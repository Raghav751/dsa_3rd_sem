#include<stdio.h>
#include<stdlib.h>

int main()
{
  int a[10][10];
  int n,i;
  int m[10];
  printf("enter number of localities:");
  scanf("%d",&n);
  for(j=0;j<n;j++)
  {
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[j][i]);
    }
  }
  
}
